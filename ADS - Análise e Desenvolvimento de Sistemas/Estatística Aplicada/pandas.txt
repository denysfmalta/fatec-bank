import pandas as pd

tabela = pd.read_excel("dvendas.xlsx")

display(tabela)

tabela.describe()

# Carregar o arquivo Vendas para um DataFrame
assimetria = skre(tabela["Qtd_Vendida"])
curtose = kurtosis(tabela["Qtd Vendida"])

print("Assimetria:", assimetria)
print("Curtose:", curtose)